<%= form_with(model: t_order, local: true) do |form| %>
  <% if t_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(t_order.errors.count, "error") %> prohibited this t_order from being saved:</h2>

      <ul>
      <% t_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
